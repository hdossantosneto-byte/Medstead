"use client";

/**
 * Marketing chrome uses the header + hamburger. Keep this module so older
 * imports stay valid, but do not pin an app-style tab bar on public pages.
 */
export function PublicBottomNav() {
  return null;
}
